package org.example.pages;

/**
 * Base class for all page objects in the test framework.
 * <p>
 * This class serves as the parent for all page object classes, following the
 * Page Object Model (POM) design pattern. Page objects should extend this class
 * and define their web elements using {@code @FindBy} annotations.
 * </p>
 *
 * @author Andrei Stoica
 * @version 1.0
 */
public class Page {

    /**
     * Default constructor for the base Page class.
     */
    public Page() {
    }
}
